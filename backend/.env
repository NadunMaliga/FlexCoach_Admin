# FlexCoach Admin Backend Environment Variables Template
# Copy this file to .env and update the values with your actual credentials

# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration (REQUIRED)
# Get this from your MongoDB Atlas dashboard
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/DATABASE_NAME

# Authentication & Security (REQUIRED)
# Generate a strong JWT secret: openssl rand -base64 64
JWT_SECRET=your_super_secure_jwt_secret_here_minimum_32_characters

# Admin Credentials (REQUIRED)
# Use a secure email and generate password hash with bcrypt
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD_HASH=$2b$12$your_bcrypt_hashed_password_here

# API Configuration
API_BASE_URL=http://localhost:3001
CORS_ORIGIN=http://localhost:3000,http://localhost:19006

# File Upload Limits (in bytes)
MAX_FILE_SIZE=10485760
MAX_PROFILE_PICTURE_SIZE=5242880

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
UPLOAD_RATE_LIMIT_MAX=10

# Development Settings
DEBUG_MODE=true
VERBOSE_LOGGING=true

# Optional: IP Whitelist (comma-separated)
# ALLOWED_IPS=127.0.0.1,::1

# Optional: Additional Security
# SESSION_SECRET=your_session_secret_here
# ENCRYPTION_KEY=your_encryption_key_here